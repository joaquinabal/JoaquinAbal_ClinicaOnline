import { LoginAvatarHoverDirective } from './login-avatar-hover.directive';

describe('LoginAvatarHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginAvatarHoverDirective();
    expect(directive).toBeTruthy();
  });
});
