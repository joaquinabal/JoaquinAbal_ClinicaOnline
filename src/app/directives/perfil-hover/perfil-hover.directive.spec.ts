import { PerfilHoverDirective } from './perfil-hover.directive';

describe('PerfilHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new PerfilHoverDirective();
    expect(directive).toBeTruthy();
  });
});
