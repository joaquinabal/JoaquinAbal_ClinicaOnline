import { RolColorDirective } from './rol-color.directive';

describe('RolColorDirective', () => {
  it('should create an instance', () => {
    const directive = new RolColorDirective();
    expect(directive).toBeTruthy();
  });
});
