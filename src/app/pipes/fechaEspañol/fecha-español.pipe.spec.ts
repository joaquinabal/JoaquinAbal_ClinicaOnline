import { FechaEspañolPipe } from './fecha-español.pipe';

describe('FechaEspañolPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaEspañolPipe();
    expect(pipe).toBeTruthy();
  });
});
